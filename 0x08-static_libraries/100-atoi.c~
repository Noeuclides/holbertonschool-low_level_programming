#include "holberton.h"

/**
*_atoi - change the value of a string point to
*
*@s                 : variable
*
*Return: character
*/

int _atoi(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (s);
}
