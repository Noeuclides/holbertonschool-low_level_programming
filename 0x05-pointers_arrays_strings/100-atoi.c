#include "holberton.h"

/**
*_atoi - change the value of a string point to
*
*@s                 : variable
*
*Return: character
*/

int _atoi(char *s)
{
	int i, j;

	for (i = 0; src[i] != '\0'; i++)
	{
	}


	for (j = 0; j <= i; j++)
	{
		*(dest + j) = *(src + j);
	}

	return (dest);
}
