#include "holberton.h"

/**
*print_rev - print string
*
*@str: a character variable
*
*/

void print_rev(char *s)
{
	int i;
	int len;

	len = 0;

	for (i = -1; i < len; i++)
	{
		if ((str[len] == '\0'))
		{
			len = i;
		}
		else
		{
			_putchar(str[len]);
			len++;
		}

	}

	_putchar('\n');

}
