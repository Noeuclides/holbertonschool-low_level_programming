#include "holberton.h"

/**
 *is_palindrome - primer numbers recursive
 *
 *@s: string to be evaluate
 *
 *Return: 1 if string is palindrome,, 0 otherwise
 */

int is_palindrome(char *s)
{
	int m;

	if (n / 2 > 1)
	{
		m = _sqrt_recursion(n / 2);
		return (funaux(m, n));
	}
	else if (n / 2 == 1 || n / 2 == 0)
	{
		return (1);
	}
	else
	{
		return (-1);
	}
}
